; ARI (res_ari) is compiled in but left disabled — AMI is the only control
; channel the webapp uses. Flip enabled=yes and add TLS if ARI is needed
; later; never expose this beyond Tailscale.
[general]
enabled = no
bindaddr = 127.0.0.1
bindport = 8088
