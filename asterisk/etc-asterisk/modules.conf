[modules]
autoload = yes

; chan_sip was removed upstream in favour of chan_pjsip; nothing to noload.
; Everything required (chan_pjsip, res_pjsip*, app_voicemail, cdr_pgsql,
; res_musiconhold, app_dial, app_queue, res_ari*) is selected at build time
; via menuselect and autoloads by default.
